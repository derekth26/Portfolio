// Hamburger Menu (Show / Hide)

// Because this is the only JavaScript we'll use for the course, we are 
// keeping it as simple as possible (and not defining any constants or variables).

// document.querySelector('.menu-controls').addEventListener('click', () => {
//     document.querySelector('body').classList.toggle('show-menu');
// });